spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gone?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 1234

  flyway:
    enabled: true

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect



server:
  port: 9091