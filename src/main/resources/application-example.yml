spring:
  datasource:
    url: jdbc:mysql://localhost:3306/{db이름}?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: 여기에 본인 로컬 MySQL 비밀번호

  flyway:
    enabled: true

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect