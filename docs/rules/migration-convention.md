# Flyway 마이그레이션 버전 규칙

## 새 마이그레이션 = 타임스탬프 버전
새로 추가하는 마이그레이션은 순번(`V1`, `V2`, ...) 대신 타임스탬프 버전을 쓴다.

```
V{yyyyMMddHHmmss}__{설명}.sql
```

예: `V20260824151700__add_outing_ping_location.sql`

- 타임스탬프는 항상 KST(Asia/Seoul) 기준이다. 로컬 머신 시계가 KST가 아닐 수 있으므로
  (WSL/Docker, 해외 위치 등은 기본값이 UTC인 경우가 많다) OS 타임존 설정에 기대지 말고
  다음처럼 명시적으로 KST로 변환해서 얻는다(둘 다 실제로 검증됨):
  - Node(가장 안전 — OS 타임존 설정과 무관하게 항상 정확함):
    `node -e "console.log(new Date(Date.now()+9*3600*1000).toISOString().slice(0,19).replace(/[-T:]/g,''))"`
  - PowerShell: `[TimeZoneInfo]::ConvertTimeBySystemTimeZoneId((Get-Date), 'Korea
    Standard Time').ToString('yyyyMMddHHmmss')`

  bash의 `TZ=Asia/Seoul date ...`처럼 OS/셸의 타임존 데이터베이스에 의존하는 방식은
  환경에 따라 오히려 잘못된 값(예: UTC)을 낼 수 있어(이 저장소의 Windows+Git Bash
  조합에서 실제로 확인됨) 권장하지 않는다 — 반드시 얻은 값이 실제 한국 시각과 맞는지
  눈으로 한 번 확인한다.
- 브랜치 생성 시점에 미리 번호를 확정하지 않는다 — 실제로 마이그레이션 파일을 커밋하는
  순간의 타임스탬프를 쓴다.

## 왜 순번을 버리는가
여러 브랜치가 동시에 진행되는 협업 환경에서 "다음 빈 번호"를 각자 브랜치 기준으로
판단하는 순번 방식은, 병합 순서가 브랜치 생성 순서와 다르면 버전이 겹친다 — 실제로
`dev`에 먼저 병합된 #94가 `V16`을 쓰는 동안 그보다 먼저 분기된 #43도 같은 `V16`을 써서
CI에서 Flyway가 컨텍스트 로딩 자체를 실패시켰다(outing과 무관한 다른 도메인 테스트까지
연쇄 실패, #105). 타임스탬프 버전은 Flyway가 팀 개발용으로 공식 권장하는 방식이고,
같은 초에 두 마이그레이션이 동시에 커밋되는 경우가 아니면 충돌 가능성이 사실상 없다.

## 기존 V1~V17은 그대로 둔다
이미 각 환경(로컬/CI/staging)에 적용된 이력이 있는 `V1`~`V17`(순번 방식)은 재번호하지
않는다 — 되돌리면 각 환경의 `flyway_schema_history`와 어긋나 더 큰 문제가 된다.

Flyway는 버전을 점(`.`)/밑줄(`_`)로 나눠 숫자로 비교하므로, 순번(예: `17`)과 타임스탬프
(예: `20260824151700`)가 섞여 있어도 항상 타임스탬프 쪽이 더 큰 값으로 정렬된다 — 순번과
타임스탬프 버전이 같은 폴더에 섞여 있어도 문제없이 공존한다.

## 관련 문서
[branch-workflow.md](./branch-workflow.md) 2단계(기획서 생성)의 "데이터 모델 변경" 절 —
새 마이그레이션이 필요한 이슈의 기획서를 쓸 때 이 문서의 버전 형식을 따른다.
